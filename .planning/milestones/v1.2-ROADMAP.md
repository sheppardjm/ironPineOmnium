# Milestone v1.2: Scoring Integrity

**Status:** ✅ SHIPPED 2026-04-14
**Phases:** 18-19
**Total Plans:** 2

## Overview

Prevent sandbagging by validating that submitted activities cover the full route distance and were recorded from the start — keeping moving-time scoring intact while adding fetch-time validation gates that reject partial rides, late starts, and hidden-start-time privacy obfuscation.

The scoring formula, leaderboard display, score preview, and athlete JSON schema were left entirely untouched. All work was additive validation in the fetch pipeline.

## Phases

### Phase 18: Configuration Foundation

**Goal**: All event constants needed for validation are in a single shared module, and the Strava fetch function extracts the two new fields (`distance`, `start_date`) that validation gates will inspect.

**Depends on**: Phase 17 (v1.1 complete)

**Requirements**: CONFIG-01, CONFIG-02

**Success Criteria** (what must be TRUE):
1. `src/lib/event-config.ts` exists and exports the gun start epoch, start-time window, and per-day distance thresholds as named constants — no magic numbers appear anywhere else in the codebase
2. `netlify/functions/strava-fetch-activity.js` reads `distance` and `start_date` from the Strava API response and makes both available for downstream validation
3. A local build (`astro build`) succeeds with no TypeScript errors after the new module is introduced

**Plans**: 1 plan

Plans:
- [x] 18-01: Create event-config.ts module, extend strava-fetch-activity.js with distance/startDate extraction, and wire submit-confirm.astro transport layer

**Details:**
- Created `src/lib/event-config.ts` as the single source of truth for all event validation constants — GUN_EPOCH_SECONDS (1780747200), START_WINDOW_SECONDS (1800), DAY1_MIN_DISTANCE_METERS (156000), DAY2_MIN_DISTANCE_METERS (153000), DAY1_DATE, DAY2_DATE, and EVENT_DATES
- Removed hardcoded `["2026-06-06", "2026-06-07"]` array from strava-fetch-activity.js and imported EVENT_DATES from event-config.ts instead
- Extended strava-fetch-activity.js Step 8 return payload with `distanceMeters` (from `activity.distance`) and `startDate` (from `activity.start_date`)
- Extended submit-confirm.astro with hidden form fields `h-distanceMeters` and `h-startDate`, Payload type optional fields, and `populateHiddenFields` set() calls
- Follow-up fix: replaced hardcoded date strings in submit-result.js with event-config imports

---

### Phase 19: Fetch Pipeline Validation Gates

**Goal**: Activities that are too short or started outside the allowed window are rejected at fetch time with errors that tell the rider exactly what was wrong and what the threshold is — no invalid data ever reaches the data store.

**Depends on**: Phase 18 (event-config.ts constants must exist before validation logic can reference thresholds)

**Requirements**: VAL-01, VAL-02, VAL-03, VAL-04, VAL-05

**Success Criteria** (what must be TRUE):
1. Submitting a Day 1 activity shorter than 156 km is rejected at fetch time with an error message that states the rider's recorded distance and the 156 km minimum
2. Submitting a Day 2 activity shorter than 153 km is rejected at fetch time with an error message that states the rider's recorded distance and the 153 km minimum
3. Submitting a Day 1 activity where `start_date` ends in `T00:00:01Z` (Strava "Hide Start Time") is rejected with a clear message before any further processing
4. Submitting a Day 1 activity where `start_date` is more than 30 minutes after the 8:00 AM ET gun time is rejected with a message that states the rider's actual start time and the allowed window
5. The submit page (`submit.astro`) surfaces each new error code (`distance_too_short`, `start_too_late`, `hidden_start_time`) as a human-readable message matching the above criteria

**Plans**: 1 plan

Plans:
- [x] 19-01: Add validation gates to strava-fetch-activity.js and error message handlers to submit.astro

**Details:**
- Added three ordered validation gates to strava-fetch-activity.js (Step 7.5) that run after Phase 18's field extraction and before the success return
- **VAL-04**: Rejects activities with hidden start time (T00:00:01Z) before any epoch arithmetic to prevent false start-time failures
- **VAL-01/02**: Rejects activities under 156 km (Day 1) or 153 km (Day 2) with the rider's actual distance and the day's minimum
- **VAL-03**: Rejects Day 1 activities starting more than 30 minutes after the 8:00 AM ET gun — Day 2 is fully exempt
- All three error codes render human-readable messages on submit.astro with the rider's actual value and the required threshold
- Gate ordering established: privacy/obfuscation checks (hidden_start_time) before correctness checks (distance) before window checks (start_too_late)

---

## Milestone Summary

**Decimal Phases:** None.

**Key Decisions:**
- v1.2 scope revision: Moving time scoring is NOT replaced — validation gates only (distance + start time window)
- Day 1 minimum distance 156 km (95% of ~164 km / ~102 miles route)
- Day 2 minimum distance 153 km (95% of ~161 km / ~100 miles route)
- Day 1 start window — reject if start_date > 30 minutes after 8:00 AM ET gun time
- Hidden start time detection — reject if start_date ends in `T00:00:01Z`
- Phases reduced from 3 to 2 — no Phase 20 (scoring formula/UI) because scoring is unchanged
- Files NOT touched: scoring.ts, leaderboard display, score preview, athlete JSON schema, athlete-loader.ts
- event-config.ts naming: follows segments.ts pattern (named exports, as const, no default export)
- Validation at fetch time only — no double-validation in submit-result.js
- HTTP 200 with error code for all user-recoverable validation failures
- Gun epoch: June 6 2026 08:00:00 EDT = 12:00:00 UTC = 1780747200
- Zero new npm packages for v1.2 implementation

**Issues Resolved:**
- Submit-result.js hardcoded dates (`2026-06-06` / `2026-06-07`) replaced with event-config imports during Phase 18 follow-up
- DAY2_DATE added to event-config import in strava-fetch-activity.js per plan spec

**Issues Deferred:**
- `distanceMeters` / `startDate` transported through submit-confirm hidden fields but dropped from submit-result POST payload — not persisted in athlete JSON. Validation fires at fetch time, so this is non-blocking, but the data could power future audit trails (deferred to a future milestone if needed)
- `strava_api_error` code from strava-fetch-activity.js falls through to generic error handler on submit.astro — acceptable degradation

**Technical Debt Incurred:**
- `submit-confirm.astro` renderPreview still uses hardcoded `'2026-06-06'` / `'2026-06-07'` in display logic (lines 138, 140, 182, 219) — UI labels, not validation gates. Low risk unless event dates change.
- `strava-fetch-activity.js` success return uses `undefined` instead of `{}` in multiValueHeaders spread (line 273) — functionally identical, undefined properties ignored in object spread.

**Stats:**
- 2 phases, 2 plans, 4 tasks
- 5 source files modified (event-config.ts, strava-fetch-activity.js, submit-result.js, submit-confirm.astro, submit.astro)
- 115 insertions, 4 deletions
- Shipped 2026-04-14 (single-day execution across both phases)

---

*For current project status, see .planning/ROADMAP.md*
