---
phase: 14-asset-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/og-image.png]
autonomous: false

must_haves:
  truths:
    - "public/og-image.png exists at exactly 1200x630px"
    - "File size is under 500KB"
    - "Image visually combines the event logo and event name in the editorial race-poster aesthetic"
  artifacts:
    - path: "public/og-image.png"
      provides: "Branded OG image for social sharing"
      contains: "1200x630 PNG"
  key_links:
    - from: "public/og-image.png"
      to: "Phase 15 OG/Twitter meta tags"
      via: "absolute URL reference (https://ironpineomnium.com/og-image.png)"
      pattern: "og-image\\.png"
---

<objective>
Create the branded OG image (1200x630px) for social sharing previews.

Purpose: When Iron & Pine Omnium links are shared on social media, the preview card must show a distinctive, on-brand image that identifies the event at a glance. This is Phase 14's design deliverable.

Output: `public/og-image.png` — a 1200x630px PNG under 500KB.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-asset-creation/14-RESEARCH.md
@public/logo.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design and create OG image</name>
  <files>public/og-image.png</files>
  <action>
Create a 1200x630px OG image for the Iron & Pine Omnium event using a programmatic approach (Node.js canvas, sharp compositing, or browser-based export).

**Use the `frontend-design` skill** for creative execution of this design. The image should feel consistent with the site's editorial race-poster aesthetic — not a generic social card.

Design constraints (WHAT, not HOW — creative latitude applies):
- Canvas: exactly 1200x630px
- Must incorporate the event logo (from public/logo.svg) and event name "Iron & Pine Omnium"
- Include supporting text: "June 6–7, 2026 · Hiawatha National Forest"
- Design palette: --color-surface (#f5f0e8), --color-night-900 (#131c1a), --color-ember-400 (#dd8258)
- Fonts: Spectral (display), Karla (body)
- Keep important content within center 80% safe zone (outer ~60px may be cropped by platforms)
- High contrast for legibility at small sizes (social cards often render 600px wide)
- File size: under 500KB (target under 300KB; simple designs typically hit 80-200KB)
- Output format: PNG

The logo SVG viewBox is 0 0 1294 966 (nearly 4:3). On the 16:9 canvas, the logo should support rather than dominate — the event name in Spectral carries primary visual weight.

**SVG rendering note:** The logo uses CSS class-based fills (.st0-.st7 with grays and dark fills). If rendering the SVG programmatically, verify it renders correctly — the CSS style block inside the SVG may not be processed by all rasterizers. If rendering is wrong, convert to PNG first via ImageMagick: `magick -density 300 -background none public/logo.svg /tmp/logo-raster.png`
  </action>
  <verify>
```bash
# Verify file exists and check dimensions
file public/og-image.png
magick identify public/og-image.png  # Should show 1200x630
ls -lh public/og-image.png  # Should be under 500KB
```
  </verify>
  <done>public/og-image.png exists at 1200x630px, under 500KB, with the event logo and name visible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>OG image at public/og-image.png — the branded social sharing preview image for all Iron & Pine Omnium links</what-built>
  <how-to-verify>
1. Open public/og-image.png in Preview (or a browser)
2. Confirm it reads as an event identity card — logo, "Iron & Pine Omnium", date/location
3. Check that it looks good at both full size (1200x630) and scaled down to ~600x315 (half size, typical mobile social card render)
4. Verify the aesthetic matches the site's editorial race-poster feel
5. Check that text is legible and important content is within the center ~80% safe zone
  </how-to-verify>
  <resume-signal>Type "approved" or describe what needs to change</resume-signal>
</task>

</tasks>

<verification>
- `magick identify public/og-image.png` reports 1200x630 PNG
- `ls -lh public/og-image.png` reports under 500KB
- Visual inspection confirms event branding is present and legible
</verification>

<success_criteria>
public/og-image.png exists at exactly 1200x630px, under 500KB, combining the event logo and name with the editorial race-poster aesthetic. The user has visually approved the design.
</success_criteria>

<output>
After completion, create `.planning/phases/14-asset-creation/14-01-SUMMARY.md`
</output>
