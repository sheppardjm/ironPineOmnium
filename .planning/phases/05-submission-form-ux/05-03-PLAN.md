---
phase: 05-submission-form-ux
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "The full submit flow is visually coherent: submit page, confirm page, and cancel all work"
    - "Score preview renders correctly for a sample payload"
    - "Form validation prevents empty name or unselected category"
    - "Cancel returns to submit page"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete submission form UX end-to-end by building the site and testing the flow with a synthetic payload.

Purpose: Confirm that all Phase 5 success criteria are met before moving to Phase 6 (Scoring Extraction) and Phase 7 (Data Persistence).
Output: Verified flow with no blocking issues.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-submission-form-ux/05-01-SUMMARY.md
@.planning/phases/05-submission-form-ux/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build site and verify static output</name>
  <files></files>
  <action>
Run `pnpm build` and verify:
1. Build completes without errors
2. `dist/submit/index.html` exists
3. `dist/submit-confirm/index.html` exists
4. Both files contain expected content (form elements, script tags)

Then create a test payload that simulates a successful strava-fetch-activity response:

```javascript
// Day 1 activity (Saturday June 6)
const day1Payload = {
  activityId: "12345678",
  athleteId: "99999",
  movingTimeSeconds: 19935,  // 5:32:15
  startDateLocal: "2026-06-06",
  sectorEfforts: {},
  komSegmentIds: []
};

// Day 2 activity (Sunday June 7)
const day2Payload = {
  activityId: "12345679",
  athleteId: "99999",
  movingTimeSeconds: 14400,  // 4:00:00
  startDateLocal: "2026-06-07",
  sectorEfforts: {
    "41159670": 482,
    "24479292": 1543,
    "24479426": 312,
    "24479467": 289
  },
  komSegmentIds: ["24479270", "16438243"]
};
```

Base64url-encode both payloads manually (using Node.js or bash) and record the resulting URLs:
- `/submit-confirm?payload=<day1-base64url>`
- `/submit-confirm?payload=<day2-base64url>`

These URLs can be used in the checkpoint verification step.
  </action>
  <verify>
`pnpm build` exits 0. Both dist pages exist. Test payload URLs are generated.
  </verify>
  <done>
Build succeeds and test URLs are ready for visual verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete submission form UX:
1. Submit page (/submit) — activity URL input with error handling and auth redirect
2. Confirm page (/submit-confirm) — score preview with Day 1/Day 2 breakdown, identity form, cancel flow
3. OAuth callback redirects to /submit after authentication

Test the flow by opening the site locally and navigating to the confirm page with the synthetic test payloads. The submit page can be tested by visiting /submit directly.
  </what-built>
  <how-to-verify>
Start the dev server: `volta run --node 22.22.2 npx netlify dev --no-open`

**Test 1 — Submit page layout:**
1. Visit `http://localhost:8888/submit`
2. Verify: centered card with "Paste your Strava activity URL" heading
3. Verify: URL input field and "Look up activity" button are styled (dark background, proper fonts)
4. Verify: form looks visually coherent with the rest of the site (same card style as /error page)

**Test 2 — Confirm page with Day 2 payload:**
1. Visit `http://localhost:8888/submit-confirm?payload=<day2-base64url>` (URL from Task 1)
2. Verify: "Day 2 — Sunday, June 7" date display
3. Verify: Day 1 section shows appropriate message for Day 2 activity (moving time is shown or noted as N/A)
4. Verify: Day 2 Sectors shows "4 of 7 sectors" with formatted time and "45% of your overall score" explanation
5. Verify: Day 2 KOM shows "2 of 3 KOM segments" with "20% of your overall score" explanation
6. Verify: Display name input and category dropdown are present and styled
7. Verify: "Confirm Submission" and "Cancel" buttons are visible

**Test 3 — Confirm page with Day 1 payload:**
1. Visit `http://localhost:8888/submit-confirm?payload=<day1-base64url>` (URL from Task 1)
2. Verify: "Day 1 — Saturday, June 6" date display
3. Verify: Day 1 section shows "05:32:15" moving time with "35% of your overall score" explanation
4. Verify: Day 2 sections show "no sectors/KOM matched" messages

**Test 4 — Form validation:**
1. On the confirm page, click "Confirm Submission" without entering a name
2. Verify: browser shows validation message for the name field
3. Enter a name, leave category unselected, click submit
4. Verify: browser shows validation message for the category field

**Test 5 — Cancel flow:**
1. On the confirm page, click "Cancel"
2. Verify: navigates to /submit

**Test 6 — Missing payload redirect:**
1. Visit `http://localhost:8888/submit-confirm` (no payload param)
2. Verify: redirects to /submit

**Test 7 — OAuth callback redirect (if Strava credentials available):**
1. Visit `http://localhost:8888/api/strava-auth`
2. After OAuth, verify you land at /submit (not /)
(Skip if local OAuth is not configured)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 5, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All Phase 5 success criteria from ROADMAP.md:
1. On first submission, the rider is asked to provide display name and category — form has required name input and category select
2. Score preview shows Day 1 moving time score and Day 2 sector + KOM scores as computed values with weight labels
3. Score preview includes inline explanation with actual values and point conversion context
4. Cancel returns to /submit cleanly
</verification>

<success_criteria>
- Human verifies all 7 test scenarios pass
- Phase 5 success criteria from roadmap are all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-submission-form-ux/05-03-SUMMARY.md`
</output>
