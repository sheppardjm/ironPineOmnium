---
phase: 01-compliance-and-prerequisites
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/segments.ts
autonomous: false
must_haves:
  truths:
    - "All Day 2 timed sector segment IDs are listed in src/lib/segments.ts with human-readable labels"
    - "All Day 2 KOM segment IDs are listed in src/lib/segments.ts with human-readable labels"
    - "Each segment ID is stored as a string type, not a number"
    - "The constants file exports typed arrays with as-const assertions for type safety"
    - "Each segment ID has been verified as existing in the Strava segment database"
  artifacts:
    - path: "src/lib/segments.ts"
      provides: "Day 2 timed sector and KOM segment ID constants with labels"
      exports: ["SECTOR_SEGMENT_IDS", "KOM_SEGMENT_IDS", "SEGMENT_LABELS", "ALL_SCORED_SEGMENT_IDS"]
  key_links:
    - from: "src/lib/segments.ts"
      to: "src/lib/types.ts"
      via: "Type alignment"
      pattern: "as const"
---

<objective>
Collect the Day 2 Strava segment IDs from the user, verify each one exists in the Strava segment database, and write them into a typed constants file at `src/lib/segments.ts`. This file will be used by the scoring extraction logic (Phase 6) to filter segment efforts from submitted activities.

Purpose: Segment IDs are a hard data prerequisite for the callback function's filtering logic. They must be confirmed and committed before any scoring code references them.
Output: `src/lib/segments.ts` with verified, typed segment ID constants.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compliance-and-prerequisites/01-CONTEXT.md
@.planning/phases/01-compliance-and-prerequisites/01-RESEARCH.md
@src/lib/types.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Provide the Day 2 Strava segment IDs for timed sectors and KOM segments</decision>
  <context>
The phase context states that segment IDs are already known. The user needs to provide:
1. The list of **timed sector** segment IDs (used for the 45% sector scoring weight)
2. The list of **KOM** segment IDs (used for the 20% KOM scoring weight)

For each segment, provide:
- The Strava segment ID (numeric, from the segment URL: `https://www.strava.com/segments/{id}`)
- A human-readable name/label for the segment (e.g., "Sector 1: Forest Ridge Climb")

Note: These are MK Ultra (Day 2 grinduro) segments specifically.
  </context>
  <options>
    <option id="provide-ids">
      <name>Provide segment IDs now</name>
      <pros>Unblocks the constants file creation immediately</pros>
      <cons>None — IDs are already known per phase context</cons>
    </option>
  </options>
  <resume-signal>Provide the sector segment IDs and KOM segment IDs with their labels.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the segments constants file</name>
  <files>src/lib/segments.ts</files>
  <action>
Create `src/lib/segments.ts` with the segment IDs provided by the user. Follow this structure:

```typescript
/**
 * Day 2 (MK Ultra) Strava Segment IDs
 *
 * These segment IDs are used to extract timed sector efforts and KOM points
 * from submitted Day 2 activities. Each ID has been verified against the
 * Strava segment database as of YYYY-MM-DD.
 *
 * Segment IDs are strings to avoid JavaScript number precision issues.
 */

/** Timed sector segments — used for the 45% sector scoring weight */
export const SECTOR_SEGMENT_IDS = [
  "XXXXXXXXX", // Sector 1: [name] — verified YYYY-MM-DD
  // ... one entry per sector
] as const;

/** KOM segments — used for the 20% KOM scoring weight */
export const KOM_SEGMENT_IDS = [
  "XXXXXXXXX", // KOM: [name] — verified YYYY-MM-DD
  // ... one entry per KOM segment
] as const;

/** Human-readable labels for each scored segment */
export const SEGMENT_LABELS: Record<string, string> = {
  "XXXXXXXXX": "Sector 1: [name]",
  // ... one entry per segment (both sectors and KOMs)
};

/** Combined list of all segment IDs used in scoring */
export const ALL_SCORED_SEGMENT_IDS = [
  ...SECTOR_SEGMENT_IDS,
  ...KOM_SEGMENT_IDS,
] as const;

export type SectorSegmentId = (typeof SECTOR_SEGMENT_IDS)[number];
export type KomSegmentId = (typeof KOM_SEGMENT_IDS)[number];
```

Replace placeholder values with the actual segment IDs and names from the user's input. Use today's date for the "verified" comments.

Important:
- All IDs must be strings (quoted), not numbers
- Use `as const` for immutability and literal type inference
- Include type exports for downstream type safety
- Include inline comments with segment name and verification date
  </action>
  <verify>Run `npx tsc --noEmit src/lib/segments.ts` (or the project's TypeScript check) to confirm the file has no type errors. If the project doesn't have a standalone tsc check, read the file and manually confirm all IDs are strings, exports match the expected names, and `as const` is applied.</verify>
  <done>src/lib/segments.ts exists with all user-provided segment IDs as typed string constants, human-readable labels, and verification date comments. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `src/lib/segments.ts` exists and exports `SECTOR_SEGMENT_IDS`, `KOM_SEGMENT_IDS`, `SEGMENT_LABELS`, `ALL_SCORED_SEGMENT_IDS`
- All segment IDs are string-typed (not numbers)
- Each segment has a human-readable label in `SEGMENT_LABELS`
- File compiles without TypeScript errors
- Segment count matches what the user provided (all Day 2 segments)
</verification>

<success_criteria>
All Strava segment IDs for Day 2 scoring sectors and KOM segments are identified, verified, and written into `src/lib/segments.ts` as typed constants with human-readable labels.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compliance-and-prerequisites/01-03-SUMMARY.md`
</output>
