---
phase: 10-design-polish-and-companion-links
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/pages/error.astro
autonomous: true

must_haves:
  truths:
    - "The error page uses light-mode styling consistent with the rest of the site"
    - "The primary CTA is 'Connect with Strava again' (retry auth)"
    - "A secondary CTA takes the rider home"
    - "Error messages are contextual and clearly readable on the light background"
    - "The error page uses global button styles, not scoped overrides"
  artifacts:
    - path: "src/pages/error.astro"
      provides: "Redesigned auth error page with light-mode styling and contextual messages"
      contains: "strava-auth"
  key_links:
    - from: "src/pages/error.astro"
      to: "/api/strava-auth"
      via: "primary CTA link"
      pattern: 'href="/api/strava-auth"'
---

<objective>
Redesign the error page for the light-base theme with contextual auth error presentation.

Purpose: When OAuth fails, riders land on error.astro. Per CONTEXT.md, auth errors get their own page with a primary CTA to retry and a secondary to go home. The page must match the site's new light-mode visual language.

Output: Redesigned error.astro with light-mode styling, contextual error messages, and proper CTAs.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-design-polish-and-companion-links/10-CONTEXT.md
@.planning/phases/10-design-polish-and-companion-links/10-RESEARCH.md
@.planning/phases/10-design-polish-and-companion-links/10-01-SUMMARY.md
@src/pages/error.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign error.astro for light mode</name>
  <files>src/pages/error.astro</files>
  <action>
Rewrite error.astro to use light-mode styling. The client-side `<script>` logic (reading the `reason` query param and mapping to user-friendly messages) must be preserved exactly.

**HTML structure change:**

Replace the dark centered card layout with a centered light-mode layout. Use a centered panel approach (not split-layout — error pages are simple and don't need the split). The page should feel calm and reassuring, not alarming.

```html
<main class="page-shell">
  <div class="error-shell">
    <div class="error-card">
      <img src="/logo.svg" alt="" width="64" height="48" style="opacity: 0.6; margin-bottom: 1.5rem;" />
      <p class="error-eyebrow">Connection Error</p>
      <h1 class="error-heading">Something went wrong</h1>
      <p class="error-message" id="error-message">
        Something went wrong during the Strava connection.
      </p>
      <div class="error-actions">
        <a class="primary-button" href="/api/strava-auth">Connect with Strava Again</a>
        <a class="secondary-button" href="/">Back to Home</a>
      </div>
    </div>
  </div>
</main>
```

Note the primary CTA text change: "Try Again" becomes "Connect with Strava Again" per CONTEXT.md ("primary CTA = Connect with Strava again").

**Script block — preserve entirely.** The `<script>` block reads the `reason` query param and sets `el.textContent`. This logic is unchanged.

**Remove the old scoped `<style>` block entirely.** Replace with a minimal scoped `<style>`:

```css
.error-shell {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 80vh;
  padding: var(--page-gutter);
}

.error-card {
  max-width: 32rem;
  width: 100%;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.error-eyebrow {
  font-family: var(--font-mono);
  font-size: 0.76rem;
  font-weight: 600;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--color-ember-500);
}

.error-heading {
  font-family: var(--font-display);
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 700;
  color: var(--color-night-900);
  line-height: 0.92;
  letter-spacing: -0.04em;
}

.error-message {
  font-family: var(--font-sans);
  font-size: 1.05rem;
  line-height: 1.65;
  color: var(--color-ink-muted);
  max-width: 36ch;
}

.error-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-top: 0.5rem;
  justify-content: center;
}

/* Secondary button — light mode, centered layout */
.secondary-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.65rem 1.4rem;
  background: transparent;
  color: var(--color-night-900);
  font-family: var(--font-sans);
  font-size: 0.9rem;
  font-weight: 500;
  border: 1px solid rgba(19, 28, 26, 0.2);
  border-radius: 999px;
  text-decoration: none;
  transition: border-color 0.15s ease;
}

.secondary-button:hover {
  border-color: var(--color-night-900);
}
```

The `.primary-button` class uses the global style (dark button on light background) defined in Plan 01's global.css. Do NOT define a scoped `.primary-button` — the global one handles it.

**Critical:**
- The `id="error-message"` must be preserved (script writes to it).
- The `href="/api/strava-auth"` on the primary CTA must be preserved (same auth endpoint).
- The error message mapping in the script stays exactly the same.
- Remove the old scoped `.eyebrow` class override — the error-eyebrow class is different and uses ember color for error context.
  </action>
  <verify>Run `pnpm build`. Build succeeds. Verify `dist/error/index.html` contains `id="error-message"`. Verify it contains `href="/api/strava-auth"`. Verify the script block contains the `messages` record with all four reason codes (strava_denied, csrf_mismatch, token_exchange_failed, insufficient_scope).</verify>
  <done>Error page renders with light-mode styling: centered layout, logo, contextual error messages, "Connect with Strava Again" primary CTA, "Back to Home" secondary CTA. Script logic unchanged. No scoped button overrides.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes with zero errors
2. `dist/error/index.html` contains `id="error-message"`
3. `dist/error/index.html` contains `href="/api/strava-auth"` (retry CTA)
4. `dist/error/index.html` contains "Connect with Strava Again"
5. Script block contains all four reason code mappings
6. No scoped `.primary-button` style definition in the page `<style>` block
</verification>

<success_criteria>
- Error page renders on light background with centered layout
- Primary CTA says "Connect with Strava Again" and links to /api/strava-auth
- Secondary CTA goes home
- Error messages are contextual and readable
- Script logic for reason-code mapping preserved
</success_criteria>

<output>
After completion, create `.planning/phases/10-design-polish-and-companion-links/10-05-SUMMARY.md`
</output>
