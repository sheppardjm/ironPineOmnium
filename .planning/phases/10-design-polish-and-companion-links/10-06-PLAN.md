---
phase: 10-design-polish-and-companion-links
plan: 06
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04", "10-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All pages render correctly with the new light-base theme"
    - "No default browser form styling is visible on submit or confirm pages"
    - "The submission entry point is clearly reachable from the landing page"
    - "The leaderboard renders correctly in its dark section wrapper"
    - "No visual regressions on mobile (375px viewport)"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for the complete site redesign.

Purpose: The full site has been redesigned across 5 plans. This checkpoint verifies that the visual language is consistent, forms are properly styled, navigation works, and no regressions exist — especially on mobile. Human eyes are needed because CSS visual quality cannot be fully verified by build output alone.

Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-design-polish-and-companion-links/10-CONTEXT.md
@.planning/phases/10-design-polish-and-companion-links/10-01-SUMMARY.md
@.planning/phases/10-design-polish-and-companion-links/10-02-SUMMARY.md
@.planning/phases/10-design-polish-and-companion-links/10-03-SUMMARY.md
@.planning/phases/10-design-polish-and-companion-links/10-04-SUMMARY.md
@.planning/phases/10-design-polish-and-companion-links/10-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build production output for visual review</name>
  <files></files>
  <action>
Run `pnpm build` to generate the full production build. Then verify all expected pages exist in the dist/ output:

1. `dist/index.html` — Landing page
2. `dist/submit/index.html` — Submit page
3. `dist/submit-confirm/index.html` — Confirm page
4. `dist/error/index.html` — Error page
5. `dist/leaderboard/index.html` — Leaderboard page

Check that all five pages are present. Report any missing pages.

Also verify no build warnings related to missing assets, broken imports, or CSS compilation issues.
  </action>
  <verify>All five page files exist in dist/. Build completed with zero errors.</verify>
  <done>Production build succeeds and all five pages are generated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete site redesign from dark to light visual language across all pages: landing page with hero, podium preview, Submit Results CTA; submit page with split layout; confirm page with split layout and score preview; error page with centered layout; dedicated /leaderboard page with dark section wrapper; sticky navigation bar on all pages.</what-built>
  <how-to-verify>
Open the built site (open dist/index.html in a browser, or run a local dev server).

**Desktop verification (check all five pages):**

1. **Landing page (/)** — Verify:
   - Light/warm off-white background (not dark green/black)
   - Sticky nav bar at top with logo, Rides link, Leaderboard link, Submit Results CTA
   - Dark hero section with photo, bold typography
   - Content sections with white cards on light background
   - Photo gallery section (dark, photography-forward)
   - Leaderboard podium preview (or placeholder if no data)
   - Submit Results CTA visible after scoring section
   - No noise texture overlay
   - No floating/drifting animations
   - Fonts: Spectral for headings, Karla for body text

2. **Submit page (/submit)** — Verify:
   - Split layout: dark brand panel on left, light form on right
   - Brand panel has logo, title, description
   - URL input uses borderless-bottom style (single line under input, no box)
   - "Look up activity" button is dark on light (not light on dark)
   - No default browser input styling visible

3. **Confirm page (/submit-confirm)** — Verify:
   - Split layout matching submit page
   - Score preview cards visible (will show empty state since no payload in URL)
   - Form fields (name, category) use borderless-bottom style
   - Buttons match submit page style

4. **Error page (/error)** — Verify:
   - Centered layout with logo
   - "Connect with Strava Again" primary CTA
   - "Back to Home" secondary CTA
   - Error message text readable on light background

5. **Leaderboard page (/leaderboard)** — Verify:
   - Dark section wrapper (leaderboard on dark background within light page)
   - Nav bar visible above
   - Leaderboard table or empty state renders correctly

**Mobile verification (375px viewport — use browser DevTools device toolbar):**

6. Check landing page at 375px — content readable, no horizontal scroll, nav fits
7. Check submit page at 375px — split layout stacks (brand on top, form below)
8. Check leaderboard at 375px — table scrolls horizontally with sticky Rider column

**Typography check:**
9. Headings use Spectral (serif) — NOT Cormorant Garamond
10. Body text uses Karla (sans-serif) — NOT Sora
11. Labels/mono text use JetBrains Mono (if visible)
  </how-to-verify>
  <resume-signal>Type "approved" if the site looks good, or describe specific issues to address.</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all five pages at both desktop and 375px mobile viewport.
</verification>

<success_criteria>
- All pages render with consistent light-base visual language
- No default browser form styling visible
- Nav bar works across all pages
- Submit Results CTA clearly reachable from landing page
- Leaderboard renders correctly in dark section
- Mobile layout is readable at 375px
- Correct fonts in use (Spectral, Karla, JetBrains Mono)
</success_criteria>

<output>
After completion, create `.planning/phases/10-design-polish-and-companion-links/10-06-SUMMARY.md`
</output>
