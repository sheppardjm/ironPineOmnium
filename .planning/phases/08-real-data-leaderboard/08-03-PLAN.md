---
phase: 08-real-data-leaderboard
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A fresh build with zero athlete JSON files renders the empty leaderboard state without errors"
    - "A build with at least one both-days-complete athlete JSON file renders that rider on the leaderboard with computed scores"
    - "The 'Live results' badge appears when athlete data is present"
    - "The 'Awaiting submissions' badge appears when no athlete data is present"
    - "KOM scores are non-zero for athletes with komEfforts data"
    - "Scoring weights come from defaultScoringConfig (35/45/20) — not hardcoded in the loader"
  artifacts: []
  key_links:
    - from: "public/data/results/athletes/*.json"
      to: "dist/index.html"
      via: "build pipeline: athlete-loader -> scoreOmnium -> Leaderboard.astro -> HTML"
      pattern: "athlete.*json"
---

<objective>
Verify the complete Phase 8 pipeline by testing both the empty state and a real-data build, confirming all 7 success criteria are satisfied.

Purpose: End-to-end verification that the build-time data pipeline works correctly — from persisted athlete JSON through scoring to rendered HTML.

Output: Verified phase with all success criteria confirmed.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-real-data-leaderboard/08-RESEARCH.md
@.planning/phases/08-real-data-leaderboard/08-01-SUMMARY.md
@.planning/phases/08-real-data-leaderboard/08-02-SUMMARY.md
@src/lib/athlete-loader.ts
@src/components/Leaderboard.astro
@src/pages/index.astro
@src/lib/scoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify empty-state build</name>
  <files></files>
  <action>
1. Ensure `public/data/results/athletes/` directory is either empty or does not exist.
2. Run `pnpm build`.
3. Inspect `dist/index.html` for:
   - "Awaiting submissions" text appears in the leaderboard section heading
   - "Awaiting submissions" status badge is present
   - "Results will appear here after riders submit" (or similar empty state text) is present
   - No "Sample standings" or "sample rider data" text appears
   - No "Rowan Beck", "Ada Keene", or any sample rider names appear
   - No JavaScript errors or build failures
4. Confirm the build completed without errors.
  </action>
  <verify>
`pnpm build` exits 0. `dist/index.html` contains "Awaiting" and does not contain sample rider names.
  </verify>
  <done>Empty-state build verified: no sample data, clean empty state, no build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify live-data build with test athlete file</name>
  <files></files>
  <action>
1. Create a temporary test athlete JSON file at `public/data/results/athletes/test-smoke-phase8.json` with both days complete. Use realistic data matching the schema from `submit-result.js`:
   ```json
   {
     "athleteId": "test-smoke-phase8",
     "displayName": "Phase 8 Smoke Test",
     "category": "men",
     "day1": {
       "movingTimeSeconds": 20500,
       "activityId": "smoke-day1",
       "submittedAt": "2026-04-07T12:00:00Z"
     },
     "day2": {
       "sectorEfforts": {
         "41159670": 720,
         "24479292": 1800,
         "24479426": 450,
         "24479467": 480,
         "24479496": 2100,
         "34573011": 1920,
         "6809754": 210
       },
       "komSegmentIds": ["24479270", "41126651", "16438243"],
       "komEfforts": {
         "24479270": 300,
         "41126651": 180,
         "16438243": 540
       },
       "activityId": "smoke-day2",
       "submittedAt": "2026-04-07T13:00:00Z"
     },
     "createdAt": "2026-04-07T12:00:00Z",
     "updatedAt": "2026-04-07T13:00:00Z"
   }
   ```
2. Run `pnpm build`.
3. Inspect `dist/index.html` for:
   - "Phase 8 Smoke Test" rider name appears in the leaderboard table
   - "Live results" or "Live Results" text appears (not "Awaiting submissions")
   - "Current standings" heading appears (not "Sample standings" or "Awaiting submissions" heading)
   - Score values are present (non-zero total score)
   - The Men tab/panel contains the test rider
   - The Women and Non-Binary tabs show the per-category empty state ("No women riders yet" / "No non-binary riders yet")
4. Verify KOM score is non-zero in the rendered output (the single rider should get max KOM points since they are alone in their category).
5. **Clean up**: Remove the test file `public/data/results/athletes/test-smoke-phase8.json` after verification. Do NOT commit it.
  </action>
  <verify>
`pnpm build` exits 0. `dist/index.html` contains "Phase 8 Smoke Test" rider name and "Live" indicator. KOM score column shows a non-zero value. Test file is removed after verification.
  </verify>
  <done>Live-data build verified: test rider appears with computed scores, live indicator shows, empty categories handled gracefully, test file cleaned up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 8 real data leaderboard pipeline: athlete JSON loading, KOM ranking, empty state, live indicator, sample data removal.</what-built>
  <how-to-verify>
1. Run `pnpm build` — should succeed with zero athlete files
2. Open `dist/index.html` in a browser — the leaderboard section should show "Awaiting submissions" state with no sample riders
3. Optionally: create a test athlete JSON file in `public/data/results/athletes/` (schema shown in Task 2 above), run `pnpm build` again, and verify the rider appears on the leaderboard with scores
4. Confirm: no "Sample standings" text anywhere on the page
5. Confirm: scoring weights in the formula section still show 35/45/20
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase 8 success criteria checklist:
1. [SC-1] After a rider submits, their name and score appear on the leaderboard after rebuild — verified by test athlete file build
2. [SC-2] Sample data entirely removed; empty build shows empty state, not placeholder riders — verified by empty-state build
3. [SC-3] Clear "Live results" indicator when showing real data — verified by live-data build
4. [SC-4] Scoring engine reads athlete JSON files from `public/data/results/athletes/` at build time — verified by import.meta.glob in athlete-loader.ts
5. [SC-5] Day 1 moving time scores computed with relative scoring and 35% weight from defaultScoringConfig — verified by scoreOmnium() call with config parameter
6. [SC-6] KOM points computed by comparing komEfforts elapsed times within category — verified by computeKomPoints() in athlete-loader.ts
7. [SC-7] Scoring weights read from defaultScoringConfig, not hardcoded — verified by import of defaultScoringConfig from scoring.ts
</verification>

<success_criteria>
- Both empty-state and live-data builds succeed without errors
- All 7 phase success criteria are confirmed
- No sample data artifacts remain in the rendering path
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/08-real-data-leaderboard/08-03-SUMMARY.md`
</output>
